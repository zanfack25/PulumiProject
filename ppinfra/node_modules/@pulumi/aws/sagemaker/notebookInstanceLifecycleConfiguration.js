"use strict";
// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
Object.defineProperty(exports, "__esModule", { value: true });
exports.NotebookInstanceLifecycleConfiguration = void 0;
const pulumi = require("@pulumi/pulumi");
const utilities = require("../utilities");
/**
 * Provides a lifecycle configuration for SageMaker AI Notebook Instances.
 *
 * ## Example Usage
 *
 * Usage:
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as aws from "@pulumi/aws";
 * import * as std from "@pulumi/std";
 *
 * const lc = new aws.sagemaker.NotebookInstanceLifecycleConfiguration("lc", {
 *     name: "foo",
 *     onCreate: std.base64encode({
 *         input: "echo foo",
 *     }).then(invoke => invoke.result),
 *     onStart: std.base64encode({
 *         input: "echo bar",
 *     }).then(invoke => invoke.result),
 * });
 * ```
 *
 * ## Import
 *
 * Using `pulumi import`, import models using the `name`. For example:
 *
 * ```sh
 * $ pulumi import aws:sagemaker/notebookInstanceLifecycleConfiguration:NotebookInstanceLifecycleConfiguration lc foo
 * ```
 */
class NotebookInstanceLifecycleConfiguration extends pulumi.CustomResource {
    /**
     * Get an existing NotebookInstanceLifecycleConfiguration resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new NotebookInstanceLifecycleConfiguration(name, state, Object.assign(Object.assign({}, opts), { id: id }));
    }
    /**
     * Returns true if the given object is an instance of NotebookInstanceLifecycleConfiguration.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === NotebookInstanceLifecycleConfiguration.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["arn"] = state ? state.arn : undefined;
            resourceInputs["name"] = state ? state.name : undefined;
            resourceInputs["onCreate"] = state ? state.onCreate : undefined;
            resourceInputs["onStart"] = state ? state.onStart : undefined;
        }
        else {
            const args = argsOrState;
            resourceInputs["name"] = args ? args.name : undefined;
            resourceInputs["onCreate"] = args ? args.onCreate : undefined;
            resourceInputs["onStart"] = args ? args.onStart : undefined;
            resourceInputs["arn"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(NotebookInstanceLifecycleConfiguration.__pulumiType, name, resourceInputs, opts);
    }
}
exports.NotebookInstanceLifecycleConfiguration = NotebookInstanceLifecycleConfiguration;
/** @internal */
NotebookInstanceLifecycleConfiguration.__pulumiType = 'aws:sagemaker/notebookInstanceLifecycleConfiguration:NotebookInstanceLifecycleConfiguration';
//# sourceMappingURL=notebookInstanceLifecycleConfiguration.js.map